import 'package:flutter/material.dart';

class PointsCounterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Points counter"));
  }
}
